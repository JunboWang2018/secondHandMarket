<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.showclear.www.dao.base.user.UserDao">
    <sql id="Base_Col_List">
            user_name, password, tel, create_date
    </sql>

    <resultMap id="Base_Col_ResultMap" type="cn.showclear.www.pojo.base.UserDo">
        <result property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="password" column="password"/>
        <result property="tel" column="tel"/>
        <result property="create_date" column="createDate"/>
        <result property="modify_date" column="modifyDate"/>
    </resultMap>

    <select id="searchUser" resultMap="Base_Col_ResultMap" parameterType="java.lang.String">
        SELECT
        user_id, <include refid="Base_Col_List"/>, modify_date
        FROM T_USER
        WHERE user_name = #{userName}
    </select>

    <select id="searchUserListByCol" resultMap="Base_Col_ResultMap" parameterType="cn.showclear.www.pojo.base.UserDo">
        SELECT
        user_id, <include refid="Base_Col_List"/>, modify_date
        FROM T_USER
        <where>
            <if test="userId != null and userId != ''">
                user_id = #{userId}
            </if>
            <if test="userName != null and userName != ''">
                AND user_name = #{userName}
            </if>
            <if test="password != null and password != ''">
                AND password = #{password}
            </if>
            <if test="createDate != null and createDate != ''">
                AND  create_date = #{createDate}
            </if>
            <if test="modifyDate != null and modifyDate != ''">
                AND  modify_date = #{modifyDate}
            </if>
        </where>
    </select>

    <insert id="register" parameterType="cn.showclear.www.pojo.base.UserDo">
        INSERT INTO T_USER(<include refid="Base_Col_List"></include>) VALUES(#{userName}, #{password}, #{tel}, CURRENT_TIMESTAMP)
    </insert>


</mapper>